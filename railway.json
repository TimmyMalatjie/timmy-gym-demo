{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py create_sample_data && python manage.py collectstatic --noinput && gunicorn timmy_gym_demo.wsgi --bind 0.0.0.0:$PORT"
  }
}